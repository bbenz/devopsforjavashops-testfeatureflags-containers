apiVersion: v1
kind: Service
metadata:
    name: "devopsforjavashopstestfeatureflags-5116"
    labels:
        app: "devopsforjavashopstestfeatureflags-5116"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "devopsforjavashopstestfeatureflags-5116"